<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.xmh.web.blogserver.mapper.UserMapper">

    <insert id="insertUser">
        insert into user (user_face, username, password, email, user_state, create_time, update_time) INTO
        values (#{userFace},#{userName},#{password},#{email},#{userState},#{createTime},#{updateTime})
    </insert>

    <update id="updateUserState">
        update user set user_state=#{userState} where user_id=#{userId}
    </update>

    <update id="updateByUserId">
        update user set user_face=#{userFace},nickname=#{nickName},username=#{userName},
        email=#{email}, update_time=#{updateTime} where user_id=#{userId}
    </update>

    <update id="resetPassword">
        update user set password=#{password} where user_id=#{userId}
    </update>

    <update id="setUserRole">
        update role_user set role_id=#{roleId} where user_id=#{userId}
    </update>

    <delete id="deleteByUserId">
        delete from user where user_id=#{userId}
    </delete>

    <select id="getUserByUserName" resultType="cn.xmh.web.blogserver.model.User">
        select * from user where username=#{userName}
    </select>

    <select id="getAllUser" resultType="cn.xmh.web.blogserver.model.User">
        select * from user
    </select>

</mapper>